# Copilot / agent notes

Follow `AGENTS.md` in the repository root.

This is the **AI Drug Optimization Agent** prototype (Next.js). Prefer not changing UI or mock workflow behavior unless requested.
